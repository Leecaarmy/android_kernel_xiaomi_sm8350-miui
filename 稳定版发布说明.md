# 稳定版发布说明

本文档记录 Leeca 内核每个稳定版的中文变更、组件版本、功能状态、测试结果和已知边界。

## 命名与版本规则

- 正式内核名称：`内核版本-Leeca-g源码提交-稳定版版本号`
- 示例：`5.4.302-Leeca-gxxxxxxxxxxxx-v1.0.0`
- `g源码提交` 使用发布标签所指向提交的前 12 位，每次源码提交变化后都必须重新编译。
- 稳定版使用语义版本号：不兼容或重大更新提升主版本，新增功能提升次版本，兼容性修复提升修订版本。
- 每个稳定版建立带中文说明的 Git 标签；标签、发布说明、内核名称和编译产物必须指向同一份源码。
- 临时测试版可以在名称末尾追加测试标识，但不得作为稳定版永久发布。

## v1.0.0（2026-09-17）

### 适用设备与基础版本

- 设备：小米 11 Pro / mars / M2102K1AC。
- Linux 内核：5.4.302。
- 编译器：Ubuntu Clang 17.0.6、LLD 17.0.6。
- 发布分支：`codex/resukisu-susfs-5.4`。
- 发布提交：以 Git 标签 `leeca-kernel-v1.0.0` 指向的提交为准。

### 本版更新

- 从 Linux 5.4.289 依次兼容合并至 5.4.302，并补齐小米 SM8350 厂商树缺少的稳定补丁依赖。
- 更新 ReSukiSU 内核驱动至 `v4.2.0-rc2-0b5cffd9@ReSukiSU`，驱动版本代码为 35149。
- 集成 SUSFS `v2.3.0`，采用 ReSukiSU/SUSFS inline hook 路径。
- 集成 ReKernel-X v1.5；NoActive 可通过 Generic Netlink 与内核建立连接。
- 集成 LZ4P，设为 ZRAM 默认压缩算法；ZRAM 容量由 Android 按设备物理内存配置，本测试设备实际为 12 GiB。
- 默认 CPU 调速器设为 `schedutil`，保留 WALT 调度负载跟踪。

### 本版修复

- **深度休眠后黑屏、无法唤醒：**修复 5.4.290 稳定补丁中的 hrtimer CPU 热插拔初始化缺失。CPU 重新上线时会重新清理 `active_bases`、`hres_active`、`expires_next` 等状态并初始化 expiry lock，避免遗留定时器基状态破坏后续挂起/恢复。
- **ZRAM 标志位错误：**将压缩优先级掩码和优先级值转换为 `unsigned long` 后再左移，避免在 64 位 flags 上发生 32 位移位溢出。
- **ZRAM reset 未定义行为：**删除使用未初始化 `prio` 读取压缩器数组的无用代码。
- **DWC3 原子上下文睡眠：**移除持有 gadget 自旋锁时的睡眠等待，避免 `scheduling while atomic`。
- **DWC3 复位等待过长：**将软复位轮询改为 10 次有界毫秒级重试，避免异常硬件状态导致约两秒阻塞。
- 补齐 stable 合并涉及的 seal、pidfd、调度、SDHCI、Qualcomm GPIO、USB、网络等 5.4 兼容接口。
- 修复 Clang 构建下暴露的 USB 启动掉线和 ZRAM 运行异常路径。

### 当前功能与版本

- ReSukiSU：`v4.2.0-rc2-0b5cffd9@ReSukiSU`。
- SUSFS：`v2.3.0`。
- ReKernel-X：v1.5，NoActive 显示 `Netlink·已连接`。
- ZRAM：支持 `lz4`、`lz4p`、`zstd`，默认 `lz4p`。
- CPU 调度：`schedutil + WALT`。
- Root 与隐藏：ReSukiSU、SUSFS inline hook。
- 设备功能：Wi-Fi、蜂窝网络、USB/ADB、相机、音频、传感器、主用户和双开用户均纳入实机检查。

### 实机验证

- 所有候选版本先使用 `fastboot boot` 临时启动，没有在定位阶段写入 boot 分区。
- 主用户 0 与双开用户 999 均为 `RUNNING_UNLOCKED`，凭据加密数据和普通应用可以访问。
- NoActive 实际界面确认 ReKernel-X Netlink 已连接；ReSukiSU 提权正常。
- ZRAM 为 12 GiB且选中 `lz4p`；三个 CPU 策略均为 `schedutil`。
- 相机服务识别 10 个设备，传感器服务识别 73 个硬件传感器；音频和网络服务正常。
- 断开 USB 后完成约 3 分钟、5 分钟和 10 分钟三轮深度休眠，均可正常唤醒。
- 同一临时启动持续约 28 分钟，boot ID 未变化；深度挂起累计成功 850 次。
- `failed_resume`、`failed_resume_early`、`failed_resume_noirq` 均为 0。
- dmesg 与 crash buffer 未发现 panic、Oops、BUG、watchdog、hung task、RCU stall、lockup 或 `scheduling while atomic`。

### 已知边界

- 本次长待机验证约 28 分钟，不能代替数天真实使用和所有第三方应用组合。
- 测试机的挂起失败计数中存在 `alarmtimer.0.auto / -16 (EBUSY)`，这是闹钟事件令进入挂起阶段暂时返回设备忙；恢复失败计数为 0，系统随后可正常重试并唤醒。
- 当前用户数据的 Synthetic Password 与历史 boot 链存在绑定差异；正式 boot 使用已验证能够正常解锁当前数据的 ramdisk，只替换内核载荷。

### 发布要求

- 编译产物必须显示 `5.4.302-Leeca-g<标签提交前12位>-v1.0.0`。
- 发布前核对 Image 与 boot SHA-256，并在本地稳定版清单中记录。
- 只有临时启动、解密、基础功能和深度休眠测试全部通过后，才允许永久刷写。
